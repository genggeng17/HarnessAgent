# HarnessAgent 规约形成过程

## 1. Brainstorming 的起点

### 1.1 初始设想

初始目标是做一个能在本地项目中读取文件、修改代码、运行测试并调用真实模型的 Coding Agent。这个表述仍然过宽：普通脚本、LLM 工具包装器和完整 Agent Harness 都可以满足它的一部分。

第一轮设计先固定三个根问题：

1. 模型负责哪些判断，程序必须确定性保证哪些约束？
2. 项目的主要工程贡献是什么，而不是简单调用一次模型 API？
3. 移除真实模型后，核心机制是否仍能离线验证？

最终形成的总原则是：LLM 只提出下一步结构化 Action；Action 解析、状态转换、权限判断、工具执行、验证门禁、持久化和停机条件由 HarnessAgent 自己实现。

## 2. 关键迭代一：从“模型工具箱”收敛为 Harness 内核

### 2.1 对话节选

> **Agent：** 你想做的是给现有 Agent 框架增加几个工具，还是自己实现负责调度、安全和恢复的运行内核？
>
> **用户：** 希望自己实现核心流程，不能把 AgentLoop、治理或恢复隐藏在现成框架里。
>
> **Agent：** 那么“模型能调用文件和 Shell”还不够。项目至少要明确 Action 协议、状态机、工具注册、策略判断、验证和持久化的责任边界。你愿意牺牲并行 Agent 等功能，优先把单 Agent 闭环做完整吗？
>
> **用户：** 可以。先完成单 Agent 的纵向闭环，并行编排不作为本阶段目标。
>
> **Agent：** 核心机制能否在不联网、不使用真实 API Key 的情况下测试？
>
> **用户：** 必须可以，用 Mock LLM 驱动确定性的 Action 序列。

### 2.2 候选方案

| 方案 | 内容 | 优点 | 主要问题 |
|---|---|---|---|
| A | 基于成熟 Agent SDK 拼装工具 | 开发快、功能多 | 核心循环和治理难以归因于本项目 |
| B | 自研单 Agent Harness 内核 | 机制边界清晰、便于离线测试 | 需要自行定义协议与恢复语义 |
| C | 一开始实现并行 Agent 与 worktree | 展示效果强 | 范围过大，容易留下无调用者的空抽象 |

### 2.3 人工决定与规格变化

人工选择方案 B，并把方案 C 明确放入范围外。由此确定：

- 使用严格 JSON Action，而不是让模型输出任意自然语言命令；
- 使用可注入的 `LLMClient`，并提供按序消费响应的 Mock 实现；
- 主循环设置迭代、工具调用、反思和重复动作上限；
- M1 先交付 Mock Turn，后续每个里程碑都形成可运行的纵向切片；
- 不创建没有真实调用者的 orchestration、subagent 和 worktree 模块。

这轮迭代最终沉淀到 `SPEC.md` 的“目标与范围”“结构化 Action”“Agent 主循环”，以及 `PLAN.md` 的 T01–T03。

## 3. 关键迭代二：选择主要贡献——治理与可恢复执行

### 3.1 对话节选

> **Agent：** Coding Agent 最危险的地方不是模型答错，而是错误动作真的产生副作用。普通 Shell、越界路径、删除文件和重复执行应该由谁决定？
>
> **用户：** 不能只靠 Prompt，必须由代码判断；高风险动作要暂停并让用户审批。
>
> **Agent：** 如果模型在 ToolCall 中把自己声明为“只读工具”，Policy 是否相信它？
>
> **用户：** 不相信。工具类别、副作用和幂等性必须来自程序注册表。
>
> **Agent：** 进程在命令发出后、结果保存前崩溃，恢复时能否自动重放？
>
> **用户：** 非幂等动作不能重放，应标记结果未知并等待人工处理。
>
> **Agent：** `DENY` 能否通过用户审批变成允许？
>
> **用户：** 不能。审批只处理 `ASK`，固定拒绝不能被覆盖。

### 3.2 从二元权限到三元 Policy

最初可选的是简单的“允许 / 禁止”。讨论后发现它不能表达“动作本身合法，但需要用户确认”，因此形成三态 Policy：

- `ALLOW`：可直接派发；
- `ASK`：保存原动作并暂停，等待一次性授权；
- `DENY`：确定性拒绝，授权不能覆盖。

同时确定以下约束：

1. Workspace 解析真实路径并拒绝绝对路径、`..` 与符号链接逃逸；
2. Registry 提供可信的工具类型、副作用和幂等性元数据；
3. Dispatcher 是唯一工具执行入口；
4. 授权必须绑定原 ToolCall、参数和工作区，且最多消费一次；
5. 外部副作用前先保存 `DISPATCHING`；
6. 恢复时遇到未完成派发，进入 `EXECUTION_UNKNOWN`，不静默重放。

### 3.3 人工修正

模拟 Agent 曾提出“`FULL_AUTO` 模式允许全部 Shell，以体现全自动”。人工否决这一建议：当前项目没有系统级沙箱，`FULL_AUTO` 仍只允许已知安全路径，普通 Shell 保持 `ASK`，已知破坏性命令保持 `DENY`。

这轮迭代沉淀到 `SPEC.md` 的“治理与审批”“会话、持久化与恢复”和主要贡献说明，对应 `PLAN.md` 的 T04–T06、T10–T11。

## 4. 关键迭代三：把运行测试升级为代码级验证门禁

### 4.1 对话节选

> **Agent：** 在系统提示词里要求“改完代码后运行测试”，能否证明任务完成？
>
> **用户：** 不能。模型可能忘记测试，也可能仅声称测试通过。
>
> **Agent：** 那验证证据如何与文件状态关联？昨天的通过结果能否证明今天的修改？
>
> **用户：** 不能。每次成功写入都应产生新的 workspace revision，旧结果随即失效。
>
> **Agent：** 要不要建立 FailureCategory、RepairDirective 等复杂反馈体系，自动判断失败原因？
>
> **用户：** 第一阶段不要。Harness 只记录启动、超时、退出码和输出，让模型依据原始证据修复。
>
> **Agent：** 多个必需验证器分次运行时，什么时候可以宣布成功？
>
> **用户：** 当前 revision 的所有必需验证器都通过后才可以。

### 4.2 候选方案与决定

| 问题 | 未采用 | 采用 |
|---|---|---|
| 测试纪律 | 仅用 Prompt 提醒 | Prompt 引导 + StateMachine 强制门禁 |
| 结果关联 | 只保存最近一次测试 | `VerificationResult` 绑定 workspace revision |
| 失败分类 | Harness 推测错误根因 | 保留客观退出码、超时和输出 |
| 完成条件 | 任意测试通过即可 | 当前 revision 的全部必需验证器通过 |

决定使用薄 `VerificationResult`，不让 Harness 在第一阶段承担错误诊断。首次写入前记录已有必需验证器的基线；写入后增加 revision 并使旧结果失效；失败证据回灌模型，允许“失败—修改—再验证”；未完成验证时拒绝 `success` Final。

### 4.3 人工修正

模拟 Agent 提议把任意成功的 `run_shell` 测试命令计入验证。人工否决：可信验证器必须预先注册，并通过 `run_verification` 产生结构化结果；否则模型可以选择过窄命令绕过验收。

这轮迭代沉淀到 `SPEC.md` 的“验证闭环”和“客观反馈信号”，对应 `PLAN.md` 的 T08–T09。

## 5. 关键迭代四：真实模型上下文与长任务一致性

### 5.1 对话节选

> **Agent：** 如果 Action 解析严格，但模型看不到完整 Action Schema，会发生什么？
>
> **用户：** 它会反复产生格式错误，严格协议反而增加无效轮次。
>
> **Agent：** 每轮应提供哪些动态信息，才能让模型知道什么动作现在合法？
>
> **用户：** 当前 phase、计划、revision、验证进度、合法下一步、工具 Schema 和剩余预算。
>
> **Agent：** 跨五个文件的任务中，怎样防止原始需求被大量工具输出冲淡？
>
> **用户：** 固化任务卡，保留原始要求与禁止项；历史按预算裁剪，但不能裁掉任务卡和近期证据。
>
> **Agent：** Patch 上下文失配后，可否用 Shell 字符串替换兜底？
>
> **用户：** 不可以。应返回新文件摘要，要求重读并限制重试次数。

### 5.2 已有证据与设计收敛

真实只读实验曾用 9 次迭代、约 107 秒读取一个 README。补齐 Action Schema、工具 Schema、状态与资源预算后，同类任务降至 2 次迭代、约 14.5 秒。记账项目又暴露出重复读取、Patch 失配、接近上限才验证和需求漂移。

据此形成：

- 从代码同源生成 Action 与工具 Schema；
- 每轮注入当前状态、计划、revision、验证进度和剩余预算；
- 固化任务卡，保留原始要求、验收条件和禁止项；
- 使用相关文件工作集、批量读取和文件 SHA-256；
- 修改失败后返回最新证据，第二次失败要求完整重读，连续三次失败终止；
- 禁止使用 Shell、临时脚本或字符串替换写源码；
- 长任务中途触发验证，避免到资源耗尽前才测试。

这些内容对应 `PLAN.md` 的 T15–T17。这里的记账与库存实验属于实现阶段反馈，不冒充“实现前的陌生 Agent 冷启动”。

## 6. AI 建议的采纳、修正与拒绝

### 6.1 采纳

- 用薄 `VerificationResult` 代替复杂失败分类，保留客观证据；
- 使用 workspace revision 让旧验证结果自动失效；
- 所有 ToolCall 统一经过 Registry、Policy 和 Dispatcher；
- 在副作用发生前持久化 `DISPATCHING`；
- 使用 Mock LLM 让核心机制完全离线测试；
- 用任务卡、上下文预算、批量读取和文件快照减少长任务漂移；
- 按 M0–M6 纵向切片推进，而不是先堆叠抽象目录。

### 6.2 修正后采纳

- AI 建议提供自动模式；人工将其限制为“仍受固定拒绝与审批边界约束的自动模式”，不等同于无条件执行；
- AI 建议自动发现验证器；人工保留显式配置，并要求多个必需验证器全部通过；
- AI 建议失败后自动重试修改；人工加入文件版本、完整重读与三次上限，防止无证据循环；
- AI 建议加载项目规则；人工明确只读取根目录 `AGENTS.md` 且限制长度，不把 `.env` 放入模型上下文。

### 6.3 拒绝

- 拒绝使用现成 Agent 框架隐藏核心循环、治理、验证和恢复；
- 拒绝提前建立没有调用者的 subagent、DAG、worktree 等模块；
- 拒绝把任意 Shell 成功等同于可信验证通过；
- 拒绝允许用户授权覆盖 `DENY`；
- 拒绝 Patch 失败后改用 Shell 字符串替换源码；
- 拒绝让 Harness 在缺少可靠证据时自动猜测测试失败根因；
- 拒绝真实记账实验中模型擅自扩展的“排名占比”功能，因为它超出了原始任务。

## 7. 模拟陌生 Agent 冷启动试运行

### 7.1 隔离条件

为检验文档是否足以独立指导实现，本节模拟一个“Agent B”：

- 与主设计者没有共享对话历史或 memory；
- 只允许读取 `SPEC.md` 和 `PLAN.md`；
- 不查看当前实现、测试、Git 历史和本文件前六节；
- 从 PLAN 选择 1–2 个 task；
- 遇到会改变公共契约的不确定之处立即暂停，不凭猜测继续。

### 7.2 提供给 Agent B 的模拟指令

> 你是首次接触 HarnessAgent 的实现者。只能依据 `SPEC.md` 与 `PLAN.md` 工作，不得索取或推测历史对话。请从 PLAN 中选择 1–2 个依赖已满足的 task，以测试先行方式推进。遇到公共数据结构、状态语义或验收标准不明确时立即暂停并提问，不要自行补全产品决定。输出：所选任务、拟写失败测试、暂停问题、已完成产物和与计划的偏差。

### 7.3 试运行一：T02“Action Schema 与状态转换”

Agent B 选择 T02，因为它依赖最少，也是后续主循环的公共契约。

它可以直接确定的内容：

- Action 一共六类；
- 使用 Pydantic 严格校验并禁止额外字段；
- 非法 type、字段缺失和非法状态转换应失败；
- 目标文件和测试命令已在 PLAN 中给出。

Agent B 拟写的首批失败测试：

1. Markdown 代码块包裹 JSON 时解析失败；
2. `type` 未知时返回精确协议错误；
3. Action 出现额外字段时失败；
4. 非法计划项状态时失败；
5. 等待审批期间不能直接进入完成状态。

#### 暂停点 A：Action 身份由谁生成

> **模拟 Agent B：** `SPEC.md` 列出了 Action 的 JSON 字段，但没有说明持久化和审批绑定所需的动作身份由模型提供，还是由 Harness 在解析后生成。如果让模型提供 ID，可能伪造或复用；如果由 Harness 生成，Schema 与持久化模型需要分层。请确认。

原意：外部 Action JSON 不应获得可信身份；审批绑定使用 Harness 内部保存的原 ToolCall 及其规范化参数。

判断：这是**规格省略**，不是 Agent B 读错。当前 SPEC 描述了“绑定原动作”，但没有把“模型协议对象”与“内部持久化身份”说透。

#### 暂停点 B：完整状态转换表缺失

> **模拟 Agent B：** SPEC 给出了 `DISPATCHING` 和 `EXECUTION_UNKNOWN` 等关键状态，但没有列出所有 phase 及合法转换。我可以测试若干显然非法的转换，却不能证明状态机完整。

原意：状态转换应由 Harness 显式限制，而不是由 AgentLoop 任意赋值。

判断：这是**SPEC 与 PLAN 颗粒度不足**。PLAN 要求“定义状态转换”，但没有给出冻结后的转换表；陌生实现者可能创建与原意不同的状态集合。

#### 产出与预期差距

Agent B 能完成 Action 类型和严格解析约 70% 的设计，但会在审批身份与完整状态图处暂停，无法安全完成状态机。若强行继续，最可能的偏差是让模型控制 `action_id`，或遗漏等待、未知执行和恢复状态。

### 7.4 试运行二：T09“revision 验证闭环”

假设 T02–T08 的接口已经存在，Agent B 继续选择 T09，因为它直接验证项目的核心反馈机制。

它可以直接确定的内容：

- 成功写入后 workspace revision 增加；
- 旧 revision 的验证结果不能证明当前内容；
- 当前 revision 的全部必需验证器通过前，`success` Final 必须被拒绝；
- 验证失败证据应回灌模型，而不是立即终止整个 Turn。

Agent B 拟写的失败测试：

1. 修改后直接返回 `success` 被状态机拒绝；
2. revision 0 的通过结果不能放行 revision 1；
3. 两个必需验证器只通过一个时仍为 dirty；
4. 失败后再次修改会使此前验证进度失效；
5. 当前 revision 全部通过后允许成功结束。

#### 暂停点 C：基线失败是否禁止首次写入

> **模拟 Agent B：** SPEC 要求“首次写入前运行已有必需验证器记录基线”，但没有说明基线必须通过才能修改。如果项目原本就有失败测试，是拒绝修改，还是允许修改并把失败记录为对比证据？

原意：基线的目的在于记录既有状态；必须尝试并保存结果，但不要求基线通过后才能开始修改。最终完成仍要求当前 revision 的必需验证器全部通过。

判断：这是**措辞歧义**。陌生实现者很可能把“运行基线”实现成“基线通过门禁”，导致无法修复原本失败的项目。

#### 暂停点 D：没有发现任何验证器时如何处理

> **模拟 Agent B：** “全部必需验证器通过”在必需集合为空时，按集合逻辑可以真空成立；但用户故事又要求修改后必须运行可信测试。此时应允许完成、要求用户配置验证器，还是进入澄清状态？

原意：修改任务不能因必需验证器集合为空而自动获得验证完成；应要求显式配置可信验证器或由用户处理缺失验证路径。

判断：这是**边界条件未明确**。若直接使用 `all([])`，会形成绕过验证的缺陷。

#### 暂停点 E：哪些工具结果会增加 revision

> **模拟 Agent B：** SPEC 说“写入增加 revision”，但没有完整列举写工具。`apply_patch`、`edit_file`、`edit_files` 显然属于写入；获批的 `run_shell` 若改变文件，Harness 是否能识别并增加 revision？

原意：受控文件工具负责可靠地报告修改并推进 revision；普通 Shell 不是源码写入通道，Policy 拒绝常见 Shell 写源码方式。对于获批 Shell 的其他不可见副作用，当前系统不承诺自动检测工作区变化。

判断：前半部分可从工具表推断，后半部分属于**当前能力边界没有集中写清**。

#### 产出与预期差距

Agent B 能写出核心 revision 测试框架，但在基线失败、空验证器和 Shell 不可见写入处暂停。若凭 Python 集合语义继续，可能让空验证器集合错误地放行成功；若把基线设为必须通过，则又会阻止 Agent 修复已有失败。

### 7.5 冷启动发现汇总

| 暂停点 | 类型 | 可能造成的错误实现 | 应明确的规则 |
|---|---|---|---|
| A：Action 身份 | 公共契约省略 | 信任模型提供的 ID | 外部 Action 与内部可信身份分离 |
| B：状态转换 | PLAN/SPEC 颗粒度不足 | 状态集合或转换不完整 | 给出全量 phase 与转换表 |
| C：失败基线 | 措辞歧义 | 旧测试失败时禁止修复 | 基线要求执行和记录，不要求通过 |
| D：空验证器 | 边界缺失 | `all([])` 绕过验证 | 修改后无可信验证器不能成功 |
| E：Shell 写入 | 能力边界分散 | 假设能检测所有 Shell 副作用 | Shell 非源码写入通道，未知副作用不承诺 revision 检测 |

### 7.6 根据模拟得出的关键修订 diff
```diff
- ApprovalGrant 与原 Action 和参数绑定一次。
+ 模型输出的 Action 不携带可信授权身份；Harness 在解析后为待审批动作生成内部身份。
+ ApprovalGrant 绑定内部身份、规范化工具参数与 workspace，并且最多消费一次。
```

```diff
- 首次写入前运行已有必需验证器记录基线。
+ 首次写入前必须尝试运行并保存已有必需验证器的基线；基线可以失败，失败不阻止进入修复。
+ 最终 success 仍要求当前 revision 的全部必需验证器通过。
```

```diff
- 当前 revision 的全部必需验证器通过前，success Final 被拒绝。
+ 修改发生后，如果未发现或未配置任何可信必需验证器，不得利用空集合直接放行 success；
+ 系统应要求配置验证器或进入明确的人工处理状态。
```

```diff
- T02：定义 Action Schema 与状态转换。
+ T02：在 PLAN 或 SPEC 附录冻结完整 phase 枚举、合法转换表、等待状态和恢复状态，
+ 并为每条非法转换提供至少一个失败测试。
```