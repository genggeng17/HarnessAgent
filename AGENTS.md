# 开发约束

- 每次与 Codex 的交互，在项目根目录 `agentlog.md` 追加一到两句话记录本次工作。
- 新增或修改的代码、注释、docstring 和项目文档默认使用中文。
- 核心循环、工具分发、治理、验证、记忆与恢复机制必须由本项目实现并可离线测试，不得隐藏在 Agent 框架中。
- 按 `docs/spec.md` 的里程碑推进；每个里程碑必须形成可运行或可测试的纵向切片，不创建没有真实调用者的空模块。

